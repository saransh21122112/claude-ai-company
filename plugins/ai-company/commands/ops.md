---
description: Route a business ops/admin request to the ops-manager agent
argument-hint: Status update, record, or admin doc you need
---

Read `Company/Departments/Operations.md` from the Obsidian vault (via
`mcp__plugin_ai-company_obsidian__*`) for ops's charter, then invoke the
`ops-manager` agent with the following request, passing along any relevant
context from `Company/Projects.md`:

$ARGUMENTS
